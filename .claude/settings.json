{
  "enabledPlugins": {
    "frontend-design@claude-plugins-official": true
  },
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "file_path=$(jq -r '.tool_input.file_path' 2>/dev/null) && if echo \"$file_path\" | grep -qE '\\.(js|jsx|ts|tsx|json|css|scss|html|md|yaml|yml)$'; then npx prettier --write \"$file_path\" 2>/dev/null; fi",
            "timeout": 30
          }
        ]
      }
    ]
  }
}
